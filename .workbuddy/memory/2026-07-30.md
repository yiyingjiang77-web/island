# 2026-07-30 工作日志

## Demo2.7 任务分析
用户要求查看之前 GPT 写的代码并确定下一步。完成了全面的代码探索：

### 当前状态
- Demo2.7 任务 01 已完成（git commit `0d049dc`）：建立了累计经验配置基础设施
- Demo2.7 任务 02-07 均为 ready-for-agent 状态

### 任务 01 完成内容
- 新增 `island_level_config` 表（10级阈值：0,100,250,450,700,1000,1400,1900,2500,3200）
- 新增 `player_island_level_reward_claim` 表（唯一约束防重复发放）
- `game_player` 新增 `cumulative_exp` 列
- `IslandGrowthServiceImpl.initialize()` 在 /game/init 时做旧经验迁移 + 补发奖励
- `GameControllerGrowthHttpTest` 5 个测试覆盖新玩家、老玩家补发、并发、十级封顶

### 任务 02 需要改造的核心点
1. `GamePlayerServiceImpl.applyExperience()` 当前用旧 `player_level_config`（扣除本级阈值），需改为用 `cumulativeExp` + `island_level_config`（不扣除，只升不降）
2. 升级时需调用 `playerCropService.grantPermanent()` 和 `playerRecipeService.grantPermanent()` 发放作物和配方
3. `ExpGainResult` DTO 需扩展，加入 cumulativeExp 和逐级奖励列表
4. 三个结算入口（收获、订单交付、吧台收取）接口不变，内部实现切换
5. 需用 `selectForUpdate` 行锁 + 同一事务保证并发安全
6. 旧 `GamePlayerServiceImplTest` 用 mock 测试旧逻辑，需改为 JWT HTTP 测试或更新 mock

## Demo2.7 任务 02 完成

### 改动摘要
- 分支：`demo2.7-cumulative-exp`（从 `demo2.6-drink-bar` 切出）
- 重写 `GamePlayerServiceImpl.applyExperience()`：从旧 `player_level_config` + 扣除阈值模式 → `cumulativeExp` + `island_level_config` + 不扣除阈值
- `ExpGainResult` 字段：`(gainedExp, beforeLevel, afterLevel, currentExp, nextLevelExp, levelsGained, rewardGold)` → `(gainedExp, beforeLevel, afterLevel, cumulativeExp, nextLevelThreshold, levelsGained, levelRewards)`
- 更新所有调用方 VO：`HarvestResultVO`、`OrderFulfillmentResultVO`、`DrinkBarCollectionResultVO`、`DrinkBarTakeDownResultVO`
- 三个结算入口均使用 `selectForUpdate` 行锁 + `@Transactional`
- 升级时按等级升序幂等发放作物种植权和配方（`player_island_level_reward_claim` 唯一约束 + `DuplicateKeyException` 捕获）
- `@Lazy` 解决 `GamePlayerServiceImpl` ↔ `PlayerCropService` 循环依赖

### 遇到的问题
1. **lambda effectively-final 编译错误**：`newLevel` 在 for 循环中被修改，但被 lambda 引用。修复：引入 `final int resolvedLevel = newLevel`
2. **旧测试文件 ExpGainResult 构造函数不匹配**：`0L` (long) → `null` (List<IslandLevelRewardVO>)。涉及 `OrderFulfillmentServiceImplTest`、`PlayerLandServiceImplTest`
3. **DrinkBarControllerHttpTest 旧字段名**：`currentExp` → `cumulativeExp`；`SET exp = X` → `SET cumulative_exp = X`；旧等级奖励金币断言需移除（新系统不发金币，只发作物/配方）
4. **H2 并发限制**：H2 in-memory 不完全支持并发 `SELECT FOR UPDATE`，并发测试改为接受一个失败，只验证不重复发放

### 测试结果
- 69 个测试全部通过（含 9 个新 HTTP 测试 + 3 个单元测试 + 57 个已有测试）
- Issue `02-cumulative-experience-and-consecutive-level-ups.md` 状态更新为 resolved
