# 2026-07-31 工作日志

## Demo2.7 前端 Bug 修复

### Bug 1: 制作台"现场制作并交付"未与选中顾客联动
- **现象**: 点击不同顾客的订单后，"现场制作并交付"按钮显示的材料消耗来自制作台下方独立选择的配方，而非当前选中顾客订单的配方
- **根因**: `renderOrderActions()` 用 `selectedRecipe`（制作台区域独立选择的配方）取材料信息，而不是从 `customer.recipeId` 查对应配方
- **修复文件**: `FruitIslandClient/demo2.4-island.html`
- **改动**:
  1. `selectCustomerOrder()`: 选中顾客时同步将制作台 `selectedRecipe` 切到顾客订单对应配方，并调用 `renderCraftingStation()` 刷新下方制作台
  2. `renderOrderActions()`: 改为从 `craftRecipes.find(r=>r.recipeId===customer.recipeId)` 取配方和材料，而非 `selectedRecipe`
  3. 增加 `hasRecipe` 判断，配方未解锁时禁用"现场制作并交付"按钮

### Bug 2: 制作台只显示第一个材料，不显示全部材料
- **现象**: strawberry_cake 需要 草莓×2 + 小麦×2 + 鸡蛋×1，但前端只显示"草莓 ×2/份"，用户以为只需要草莓就能做
- **根因**: `renderCraftingStation()` 和 `renderOrderActions()` 都只取 `recipe.materials[0]`，没有遍历全部材料
- **修复**: 改为遍历 `recipe.materials` 全部材料，每种材料显示名称、需求量、库存和消耗量，材料不足时标红并禁用制作按钮
- **数据库确认**: `recipe_material` 表中 strawberry_cake 有 3 条记录（strawberry/wheat/egg），`maxCraftable=0`（因小麦和鸡蛋库存为 0），后端校验逻辑正确，纯前端展示问题

### Bug 3: 材料不足时"+"按钮仍可点击
- **现象**: 顾客订单的"现场制作"数量选择器，"+"按钮在材料不足时仍可点击，导致数量增加但无法制作
- **根因**: `renderOrderActions()` 中"+"按钮只检查 `orderCraftQuantity>=99`，未根据材料库存计算最大可制作份数
- **修复**: 在 `renderOrderActions()` 和 `changeOrderCraftQuantity()` 中增加 `maxCraftable` 计算（遍历配方所有材料，取 `floor(库存/单份需求)` 的最小值），"+"按钮在 `orderCraftQuantity>=maxCraftable` 时禁用

### Bug 4: 背包库存不足时"从背包完整交付"按钮仍可点击
- **现象**: 顾客订单要求交付 N 份产品，但背包库存不足 N 份时，"从背包完整交付"按钮仍可点击，点击后服务端报错
- **修复**: `renderOrderActions()` 中从 `drinkBarState.drinks` 查找 `customer.itemId` 对应的 `inventoryCount`，不足时禁用按钮并显示当前库存数

## 7/31 下午：世界布局讨论 & 后端重启

### 世界布局规划
- 参考概念图 `assets/art/concepts/image.png`（卡通等距农场风格），讨论 48×48 大世界布局
- **核心决策**：
  - 饮品店 + 蛋糕店 + 交易所三核心同等重要
  - 农田紧贴饮品店四周环绕（6块：草莓/小麦/苹果/菠萝/白菜/柑桔）
  - 花园（香草/玫瑰/可可）+ 蜂巢（蜂蜜）+ 鸡舍/牛棚 置于蛋糕店附近，形成甜品原料三角
  - 码头在底部作为登岛入口，更符合"小岛"叙事
  - 上方用树木遮挡待解锁区域
  - 装饰区留给玩家自由摆放
- 待定：装饰物是否有功能（吸引顾客/提升满意度）；NPC 摊位；树木后方解锁内容

### NPC 系统设计文档
- 用户确认：装饰区纯装饰；需要 NPC 摊位和互动小功能；树木后方解锁互动区域
- 编写 `prd/demo3.0-npc-and-interactive-world-spec.md`
- 5 个 NPC：阿海（渔夫）、小铃（猫商人）、花姨（花匠）、糖糖（甜品鉴赏家）、小灰（邮差鸽子）
- 好感度系统：5 级，每日赠送 1 次，最爱 +15 / 喜欢 +8 / 普通 +3 / 不喜欢 +1
- 4 个隐藏区域：温泉、秘密钓点、秘密花园、精灵树（通过 NPC 好感度 + 玩家等级 + 砍树费解锁）
- 装饰区 12 槽位，纯视觉无功能
- 4 阶段实现：Phase1 NPC 骨架 → Phase2 回报系统 → Phase3 隐藏区域 → Phase4 装饰区

### 场景改造决策：海岛 → 山谷林地
- **核心原因**：游戏涉及小麦/坚果/菌菇/鸡蛋牛奶等温带食材，热带海岛设定不合理；小动物（松鼠/刺猬/知更鸟/狐狸）不是海岛原住民
- **改造方案**：48×48 网格不变，换叙事外衣——海→河流、码头→木桥、树墙→密林深处、海钓→河钓、菠萝→温室限定
- **命名调整**：果香小岛 → 果香山谷
- **小动物 NPC**：独立于人物 NPC，是地图原住民，住自然栖息地，每日自动产出野生食材
  - 小松鼠（果园树洞）：榛果/松子/栗子
  - 刺猬（花园灌木下）：口蘑/香菇/鸡油菌/松露（好感满级）
  - 知更鸟（饮品店屋檐）：蓝莓/树莓/桑葚
  - 小狐狸（密林边缘，每周来2-3次）：薄荷/百里香/薰衣草/洋甘菊
- **动物好感度**：3 级（陌生→熟悉→亲密），双轨制——每日领礼物固定 +2，每日投喂 +1~+5（最爱+5/喜欢+3/普通+1/不喜欢+0），只领礼物 40 天满级，每天投喂最爱 13 天满级
- **菌菇食谱**：8 个配方（4 饮品 + 4 甜品），放入交易所配方商店用金币购买，松露系列为后期高价值追求
- **联动配方**：3 个跨动物食材配方，作为动物好感升级奖励自动解锁，不放入商店
- 更新 `prd/demo3.0-npc-and-interactive-world-spec.md`：新增小动物 NPC 章节、菌菇食谱章节、数据结构和 API

### 后端连接问题
- 昨天的 Java 进程（user-center:8081, game-server:8082）仍在但已僵死
- 旧进程杀掉后重启，使用 test profile（H2 内存库），`SERVER__PORT` 环境变量需用 `-Dserver.port=` 覆盖
- 重启后 user-center 和 game-server 均正常启动，JWT 签名错误是因为浏览器端旧 token 过期，刷新页面重新登录即可
