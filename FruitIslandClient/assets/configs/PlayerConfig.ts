/**
 * 玩家配置 — Demo2.3
 *
 * 尺寸/速度/阈值等通用标准 → ScaleConfig.PLAYER
 * 出生点 → MapConfig.SPAWN_GX / SPAWN_GY
 *
 * 本文件仅保留玩家独有的行为参数（技能、体力等，后续扩展）
 */
export const PlayerConfig = {
  // 预留：玩家专属行为参数
  // 例如：最大体力、技能冷却、互动距离等
} as const;
